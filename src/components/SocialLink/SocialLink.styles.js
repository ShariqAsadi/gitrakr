import styled from 'styled-components';

export const Container = styled.div`
  font-size: 2.4rem;
  font-weight: 500;
  padding: 0px 30px;

  a {
    margin-left: 0.2em;
    color: var(--clr-red);
    text-decoration: none;
    cursor: pointer;
  }
`;
